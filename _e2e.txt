==== 1. initialize temp datadir ==== 
2026-09-11T18:07:55.307532Z 0 [System] [MY-013169] [Server] D:\Mysql\mysql-8.0.46-winx64\bin\mysqld.exe (mysqld 8.0.46) initializing of server in progress as process 5168
2026-09-11T18:07:55.324021Z 1 [System] [MY-013576] [InnoDB] InnoDB initialization has started.
2026-09-11T18:07:55.912519Z 1 [System] [MY-013577] [InnoDB] InnoDB initialization has ended.
2026-09-11T18:07:57.331345Z 6 [Warning] [MY-010453] [Server] root@localhost is created with an empty password ! Please consider switching off the --initialize-insecure option.
init-finished 
==== 2. start mysqld on port 3307 ==== 
mysqld-ready 
==== 3. import sql/bbs_schema.sql ==== 
import-exit=0 
==== 4. verify tables + seed ==== 
Tables_in_bbs_app
bars
comments
favorites
follows
likes
post_images
posts
users
users	bars	posts	images	comments	likes	favs	follows
6	16	5	4	3	5	2	6
verify-exit=0 
==== 5. smoke test against the temp instance ==== 
==============================================================================
bbs-app 后端冒烟测试
==============================================================================
[1/3] 校验应用与路由
  [PASS] 核心路由全部注册
  [PASS] OpenAPI 文档可生成
  [PASS] Bearer 鉴权已写入文档
[2/3] 在 127.0.0.1:8010 启动临时服务
[bbs-app] 环境=development 端口=3307 数据库=root@127.0.0.1:3307/bbs_app SSL=off CORS=*（未配置白名单，放开所有）
[bbs-app] 上传目录=D:\项目\bbs-app-backend\uploads
[bbs-app] MySQL 连接正常，启动自检通过
  [PASS] 临时服务启动成功
[3/3] 校验接口行为
  [PASS] 健康检查返回统一响应体 {code,message,data}
      数据库状态：可用（继续跑完整业务闭环）
  [PASS] CORS 跨域头存在（access-control-allow-origin: *）
  [PASS] 未登录访问受保护接口 → 401 + 统一错误体
  [PASS] 参数不合法 → 422 + 统一错误体
  [PASS] 注册成功并返回 token
  [PASS] 登录成功并返回 token
  [PASS] 带 token 可以获取当前用户
  [PASS] 密码错误返回 401
  [PASS] 贴吧列表返回数组且字段齐全
  [PASS] 吧内帖子分页返回 bar + list + hasMore
  [PASS] 关注贴吧成功（followed=true）
  [PASS] 我关注的吧包含刚关注的吧
  [PASS] 取消关注成功（followed=false）
  [PASS] 发帖成功且返回 2 张图片
  [PASS] 图片返回 /uploads/ 地址
  [PASS] 帖子详情可读且 liked / favorited 为 false
  [PASS] 帖子点赞成功且计数 +1
  [PASS] 重复点赞不会重复计数
  [PASS] 取消点赞后计数还原
  [PASS] 收藏成功
  [PASS] 我的收藏包含刚收藏的帖子
  [PASS] 取消收藏成功
  [PASS] 发表评论成功
  [PASS] 评论列表可分页查询
  [PASS] 评论后帖子 commentCount 已同步
  [PASS] 评论点赞成功
  [PASS] 取消评论点赞成功
  [PASS] 搜索能命中刚发的帖子
  [PASS] 作者可以删除自己的帖子
  [PASS] 已删除帖子返回 404
  --- 高级管理员 / 管理员账号管理 ---
  [PASS] 高级管理员 superadmin 登录成功
  [PASS] superadmin 的 role 为 super_admin
  [PASS] 普通管理员 admin 登录成功
  [PASS] 普通管理员查管理员列表被拒（403）
  [PASS] 普通管理员新增管理员被拒（403）
  [PASS] 管理员列表包含 superadmin 与 admin
  [PASS] 列表项含 roleText / isSelf 字段
  [PASS] 高级管理员排在列表最前面
  [PASS] 新增管理员成功
  [PASS] 用户名重复时新增失败（400）
  [PASS] 非法角色被拒（400）
  [PASS] 新管理员可以登录后台
  [PASS] 新管理员（普通）访问管理员列表被拒（403）
  [PASS] 提升为高级管理员成功
  [PASS] 提升后可以访问管理员列表
  [PASS] 降级为管理员成功
  [PASS] 不能禁用自己（400）
  [PASS] 不能降级自己（400）
  [PASS] 不能撤销自己的管理员权限（400）
  [PASS] 禁用管理员成功
  [PASS] 被禁用的管理员无法登录（403）
  [PASS] 重新启用管理员成功
  [PASS] 重置管理员密码成功
  [PASS] 用新密码可以登录
  [PASS] 旧密码已失效（401）
  [PASS] 撤销管理员权限成功（降级为普通用户）
  [PASS] 被撤销后不再是管理员（详情 404）
  [PASS] 被撤销的账号无法再访问管理员接口（403）
  [PASS] 被撤销的账号仍是可登录的普通用户
  --- 后台管理补全（用户管理 / 贴吧编辑删除 / 全部评论） ---
  [PASS] 管理员 admin 登录成功（后台补全用例）
  [PASS] 普通用户 demo 登录成功（用于权限对比）
  [PASS] 普通用户访问用户管理被拒（403）
  [PASS] 用户列表返回分页结构且含演示账号
  [PASS] 用户项含 roleText / statusText / postCount
  [PASS] 按关键字筛选用户可用
  [PASS] 按角色筛选用户可用
  [PASS] 非法角色被拒（400）
  [PASS] 用户详情可读
  [PASS] 注册临时用户成功
  [PASS] 禁用用户成功
  [PASS] 被禁用用户无法登录（403）
  [PASS] 重新启用用户成功
  [PASS] 启用后可正常登录
  [PASS] 不能禁用自己的账号（400）
  [PASS] 新建临时贴吧成功
  [PASS] 编辑贴吧成功（简介已更新）
  [PASS] 改成已存在的吧名被拒（400）
  [PASS] 删除贴吧成功
  [PASS] 删除后该贴吧不可访问（404）
  [PASS] 普通用户访问全部评论被拒（403）
  [PASS] 全部评论列表可读且含所属帖子标题
  [PASS] 临时评论创建成功
  [PASS] 发表后帖子评论数 +1
  [PASS] 删除评论成功（软删除 status=0）
  [PASS] 删除后帖子评论数复原
  [PASS] 重复删除幂等（仍返回成功）
------------------------------------------------------------------------------
通过 90 项，失败 0 项
smoke-exit=0 
==== 6. shutdown temp instance + cleanup ==== 
cleanup-done 
