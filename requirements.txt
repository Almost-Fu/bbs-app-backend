# bbs-app backend dependencies (FastAPI + MySQL, raw SQL, no ORM)
# NOTE: keep this file ASCII-only - pip decodes it with the OS locale (GBK on Chinese Windows)
fastapi>=0.110
uvicorn[standard]>=0.27
pymysql>=1.1
PyJWT>=2.8
python-multipart>=0.0.9
python-dotenv>=1.0
# MySQL 8.4 / 云数据库(Aiven)默认使用 caching_sha2_password 认证：
# 走 TLS(DB_SSL=true)时不需要它；万一没开 TLS，pymysql 需要 cryptography 才能完成 RSA 密钥交换。
cryptography>=42.0
